<%= form_for [@student, @interesting_thing] do |f| %>
  <% if @interesting_thing.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@interesting_thing.errors.count, "error") %> prohibited this interesting_thing from being saved:</h2>

      <ul>
      <% @interesting_thing.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name, 'Interesting Thing' %><br>
    <%= f.text_field :name, size: 30 %>
  </div>
  <div class="field">
    <%= f.hidden_field :student_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
